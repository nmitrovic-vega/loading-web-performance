import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4370f6',
      contrastText: '#fff',
    },
    secondary: {
      main: '#d9316b',
      contrastText: '#000',
    },
  },
});
